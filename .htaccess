<IfModule mod_rewrite.c>
    Options -MultiViews

    RewriteEngine On

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^rest/ rest/RestController.php [QSA,L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule .* index.html [QSA,L]
</IfModule>